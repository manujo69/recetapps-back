spring.application.name=recipes-back

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/recipes_db
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Security
jwt.secret=recipes-back-jwt-secret-key-2024-change-in-production
jwt.expiration=86400000

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# OpenAPI
springdoc.swagger-ui.path=/swagger-ui.html

# Logbook - API request/response logging
logging.level.org.zalando.logbook=TRACE
logbook.format.style=http
logbook.obfuscate.headers=Authorization
logbook.exclude=/swagger-ui/**,/v3/api-docs/**,/actuator/**
logging.file.name=logs/recipes-back.log